<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxcia.blog.mapper.user.CommentMapper">
    <select id="getCommentLikeNumByCommentIds" resultType="com.jxcia.blog.pojo.vo.CommentMsgVo">
        select user_comment_id as id, count(*) as likeNum,
               max(case when user_id = #{userId} then 1 else 0 end) as isLiked
        from user_like_comment
        where user_comment_id in
        <foreach collection="cIdList" item="cId" open="(" close=")" separator=",">
            #{cId}
        </foreach>
        group by user_comment_id
    </select>
</mapper>