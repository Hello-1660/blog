<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxcia.blog.mapper.user.EmailMapper">

    <insert id="insertByEmailList">
        insert into email (title, content, receiver_id, sender_id, create_time, status)
        values
        <foreach collection="emailList" item="e" separator=",">
            (#{e.title}, #{e.content}, #{e.receiverId}, #{e.senderId}, #{e.createTime}, #{e.status})
        </foreach>
    </insert>
    
    <update id="update">
        update email
        <set>
            <if test="title != null and title != ''">title = #{title}, </if>
            <if test="content != null and content != ''">content = #{content}, </if>
            <if test="status != null">status = #{status}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteByIdList">
        delete from email where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>